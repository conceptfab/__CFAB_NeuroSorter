1. model:
- name:
   - architecture
   - variant
   - input_size
   - num_classes
   - pretrained

2. training:
   - batch_size
   - learning_rate
   - optimizer
   - scheduler
   - warmup_epochs
   - mixed_precision
   - unfreeze_layers
   - unfreeze_strategy

3. regularization:
   - weight_decay
   - drop_connect_rate
   - dropout_rate
   - label_smoothing
   - swa (użycie i parametry)

4. augmentation:
   - basic (ustawienia augmentacji podstawowej)
   - mixup
   - cutmix

5. preprocessing:
   - normalization

6. monitoring:
   - early_stopping (monitor, patience)
   - checkpointing (metric)